import { CollapseOnClickDirective } from './collapse-on-click.directive';

describe('CollapseOnClickDirective', () => {
  it('should create an instance', () => {
    const directive = new CollapseOnClickDirective();
    expect(directive).toBeTruthy();
  });
});
