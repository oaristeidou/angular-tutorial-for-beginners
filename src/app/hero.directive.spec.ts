import { HeroDirective } from './hero.directive';

describe('HeroDirective', () => {
  it('should create an instance', () => {
    const directive = new HeroDirective();
    expect(directive).toBeTruthy();
  });
});
